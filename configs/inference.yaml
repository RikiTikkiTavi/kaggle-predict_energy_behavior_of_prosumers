# ---------- Overriding hydra default configs ----------
hydra:
  job:
    name: eb-inference
    chdir: true
  run:
    dir: ${dir.outputs}/${phase}/${hydra.job.override_dirname}
  sweep:
    dir: ${dir.output_dir}/${hydra.job.name}/
    subdir: ${hydra.job.override_dirname}

defaults:
  - _self_
  - dir: alpha

phase: test

model:
  _target_: predict_energy_behavior.models.joined_model.JoinedModel.load
  path: /beegfs/ws/0/s4610340-energy_behavior/yahor/kaggle-predict_energy_behavior_of_prosumers/outputs/train/cst+diff-lgbms-submit/model_joined-f-2023-1

debug: True